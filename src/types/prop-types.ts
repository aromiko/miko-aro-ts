export type ImageTextCardType = {
  header: string;
  body: string;
  imageUrl: string;
  color: string;
};

export type WorkPageType = {
  company: string;
  title: string;
  tenure: string;
  body?: string;
};
